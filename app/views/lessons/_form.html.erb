<%= render('layouts/errors', :object => @lesson) %>

<%= form_for [@subject, @lesson] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.label :number %>
  <%= f.text_area :number %>
  <p><%= f.submit %></p>
<% end %>
