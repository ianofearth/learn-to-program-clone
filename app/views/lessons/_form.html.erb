<%= render('layouts/errors', :object => @lesson) %>

<%= form_for [@subject, @lesson] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <p><%= f.submit %></p>
<% end %>
